{
  "name": "static-prototype-build-tool",
  "version": "1.0.0",
  "description": "Static prototype build tool using npm",
  "devDependencies": {
    "clean-css": "^4.2.1",
    "clean-css-cli": "^4.2.1",
    "concurrently": "^4.0.1",
    "http-server": "^0.11.1",
    "jshint": "^2.9.6",
    "jslint": "^0.12.0",
    "mocha": "^5.2.0",
    "node-sass": "^4.10.0",
    "reload": "^2.3.1",
    "uglify-js": "^3.4.9",
    "watch": "^1.0.2"
  },
  "scripts": {
    "scss": "node-sass src/scss/index.scss dist/public/css/index.css",
    "lint:scss": "sass-lint -c .sass-lint.yml 'src/scss/**/*.scss' -v -q",
    "process:css": "echo '=> processing css' && npm run lint:scss -s && npm run scss -s",
    "watch:css": "watch 'npm run process:css -s' src/scss/",
    "lint:js": "jslint 'src/js/**/*.js'",
    "bundle": "uglifyjs src/js/main.js -o dist/public/js/main.js -b",
    "process:js": "echo '=> processing js' && npm run lint:js -s && npm run bundle -s",
    "watch:js": "watch 'npm run process:js -s' src/js/",
    "dev": "echo '=> building' && npm run process:css -s && npm run process:js -s",
    "server": "reload -b --dir dist/public",
    "pretest": "jslint 'test/**/*.js'",
    "test": "echo '=> testing' && mocha test/",
    "prestart": "npm run test -s && npm run dev -s",
    "start": "concurrently \"npm run watch:css -s\" \"npm run watch:js -s\" \"npm run server -s\""
  },
  "author": "Jelizaveta Potapova",
  "license": "ISC"
}
